RewriteEngine On

# Route legacy *.php URLs to the single front controller.
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^([A-Za-z0-9_-]+)\.php$ index.php?page=$1 [QSA,L]
